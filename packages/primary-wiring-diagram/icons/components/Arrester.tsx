import React from "react";
import { ReactComponent } from "../assets/arrester.svg";
import { MetaIconComponent } from "../dto";

const Asserter: MetaIconComponent = () => {
  return <ReactComponent />;
};

Asserter.metaName = "避雷";
Asserter.id = 4;

export default Asserter;
