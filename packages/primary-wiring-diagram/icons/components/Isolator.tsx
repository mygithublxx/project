import React from "react";
import { ReactComponent } from "../assets/isolator.svg";
import { MetaIconComponent } from "../dto";

const Isolator: MetaIconComponent = () => {
  return <ReactComponent />;
};

Isolator.metaName = "刀闸";
Isolator.id = 8;

export default Isolator;
