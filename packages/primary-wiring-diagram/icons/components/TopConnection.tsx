import React from "react";
import { ReactComponent } from "../assets/topConnection.svg";
import { MetaIconComponent } from "../dto";

const TopConnection: MetaIconComponent = () => {
  return <ReactComponent />;
};

TopConnection.metaName = "上接头";
TopConnection.id = 16;

export default TopConnection;
